# Sagas — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que un lector que se queda sin capítulos descubra las demás historias del mismo mundo.

**Architecture:** Una base `Sagas` en Notion y dos relaciones nuevas en `Novelas` (`Saga`, una sola, con `Orden en saga`; y `Relacionadas`, novela↔novela, para los cruces). El sync las resuelve a slugs y simetriza `Relacionadas`. Astro gana una colección `sagas` y una página `/saga/[slug]`; la lógica de orden vive en `src/lib/saga.ts`, pura y testeada, como ya hace `taxonomy.ts`.

**Tech Stack:** Astro 4 (content collections), TypeScript, vitest, Notion API (`@notionhq/client`), tsx.

**Spec:** `docs/superpowers/specs/2026-07-13-sagas-design.md`

## Global Constraints

- Rama: `feat/sagas`. **Nunca commitear a `main`** (protegida).
- Todos los campos de saga son **opcionales**: una novela sin saga debe validar y renderizar igual. `el-heraldo-gris` no tiene saga y es el canario.
- Idioma de la UI: español, con acentos correctos.
- Tests: `npm test` (vitest) desde `web/`. Los tests van junto al fuente (`x.ts` + `x.test.ts`), como ya hace el repo.
- La saga **no** se replica en capítulos.
- Texto del bloque de descubrimiento: **«Sigue leyendo en este mundo»**. Nunca «has terminado la saga» — el último capítulo publicado puede no ser el final de la historia.

---

### Task 0: Crear la base `Sagas` en Notion (manual, humano)

**No hay código en esta tarea.** El resto del plan depende de su ID.

- [ ] **Step 1: Crear la base `Sagas`** con estas propiedades exactas (los nombres se leen literalmente en el sync):

| Propiedad     | Tipo        |
|---------------|-------------|
| `Nombre`      | Title       |
| `Slug`        | Text        |
| `Descripción` | Text        |
| `Orden`       | Number      |

- [ ] **Step 2: Añadir a la base `Novelas`** tres propiedades:

| Propiedad       | Tipo                                            |
|-----------------|-------------------------------------------------|
| `Saga`          | Relation → `Sagas`, **limitada a 1 página**     |
| `Orden en saga` | Number                                          |
| `Relacionadas`  | Relation → `Novelas` (a sí misma)               |

- [ ] **Step 3: Compartir la base `Sagas` con la integración de Notion** (la misma que ya lee novelas y capítulos). Si se olvida, el sync fallará con `object_not_found`.

- [ ] **Step 4: Copiar el ID de la base.** Está en la URL: `notion.so/<workspace>/<ID de 32 hex>?v=...`. Formatearlo con guiones (8-4-4-4-12) igual que los que ya hay en `notion.ts:7-8`.

- [ ] **Step 5: Rellenar al menos una saga y una novela dentro de ella**, dejando `el-heraldo-gris` sin saga si no le corresponde. Hace falta para la verificación end-to-end de la Tarea 6.

**Entregable:** el ID de la base, que se pega en `notion.ts` en la Tarea 2.

---

### Task 1: Transformación de Notion → datos (puro, testeado)

Toda la lógica de parseo y simetría, sin tocar la red. Es la tarea con más riesgo de bug y la que mejor se testea.

**Files:**
- Modify: `web/scripts/notion-sync/types.ts`
- Modify: `web/scripts/notion-sync/transform.ts`
- Test: `web/scripts/notion-sync/transform.test.ts`

**Interfaces:**
- Consumes: nada de tareas anteriores.
- Produces:
  - `interface SagaData { slug: string; name: string; description: string; order: number }`
  - `NovelData` gana `saga: string | null`, `sagaOrder: number | null`, `related: string[]`
  - `parseSaga(page: NotionPage): SagaData`
  - `novelSlugOf(page: NotionPage): string`
  - `parseNovel(page: NotionPage, sagaSlugById?: Map<string,string>, novelSlugById?: Map<string,string>): NovelData`
  - `symmetrizeRelated(novels: NovelData[]): NovelData[]`

- [ ] **Step 1: Escribir los tests que fallan**

Añadir a `web/scripts/notion-sync/transform.test.ts`. Importar `parseSaga`, `novelSlugOf` y `symmetrizeRelated` en la línea 2, junto a lo que ya se importa:

```ts
import {
  parseNovel, parseChapterMeta, chapterSlug, parseSaga, novelSlugOf, symmetrizeRelated,
} from './transform';
import type { NotionPage, NovelData } from './types';
```

El fixture `novelPage` (líneas 5-17) es de una novela **sin** saga: déjalo intacto — es el caso canario. Añade al final del fichero:

```ts
const sagaPage: NotionPage = {
  id: 'saga-1',
  properties: {
    'Nombre': { title: [{ plain_text: 'El Ciclo de la Niebla' }] },
    'Slug': { rich_text: [{ plain_text: 'el-ciclo-de-la-niebla' }] },
    'Descripción': { rich_text: [{ plain_text: 'Un mundo velado.' }] },
    'Orden': { number: 1 },
  },
};

const sagaNovelPage: NotionPage = {
  id: 'novel-2',
  properties: {
    ...novelPage.properties,
    'Título': { title: [{ plain_text: 'La Torre Sumergida' }] },
    'Slug': { rich_text: [{ plain_text: 'la-torre-sumergida' }] },
    'Saga': { relation: [{ id: 'saga-1' }] },
    'Orden en saga': { number: 2 },
    'Relacionadas': { relation: [{ id: 'novel-1' }, { id: 'fantasma' }] },
  },
};

describe('parseSaga', () => {
  it('mapea todas las propiedades de una saga', () => {
    expect(parseSaga(sagaPage)).toEqual({
      slug: 'el-ciclo-de-la-niebla',
      name: 'El Ciclo de la Niebla',
      description: 'Un mundo velado.',
      order: 1,
    });
  });

  it('deriva el slug del nombre si falta Slug', () => {
    const noSlug: NotionPage = {
      id: 's',
      properties: { ...sagaPage.properties, 'Slug': { rich_text: [] } },
    };
    expect(parseSaga(noSlug).slug).toBe('el-ciclo-de-la-niebla');
  });

  it('usa orden 0 si falta Orden', () => {
    const noOrder: NotionPage = {
      id: 's',
      properties: { ...sagaPage.properties, 'Orden': {} },
    };
    expect(parseSaga(noOrder).order).toBe(0);
  });
});

describe('novelSlugOf', () => {
  it('devuelve el slug sin resolver relaciones', () => {
    expect(novelSlugOf(sagaNovelPage)).toBe('la-torre-sumergida');
  });
});

describe('parseNovel con sagas', () => {
  const sagas = new Map([['saga-1', 'el-ciclo-de-la-niebla']]);
  const novels = new Map([
    ['novel-1', 'el-heraldo-gris'],
    ['novel-2', 'la-torre-sumergida'],
  ]);

  it('resuelve la saga y su orden', () => {
    const n = parseNovel(sagaNovelPage, sagas, novels);
    expect(n.saga).toBe('el-ciclo-de-la-niebla');
    expect(n.sagaOrder).toBe(2);
  });

  it('resuelve las relacionadas y descarta las que no están publicadas', () => {
    // 'fantasma' no está en el mapa: es una novela sin publicar.
    expect(parseNovel(sagaNovelPage, sagas, novels).related).toEqual(['el-heraldo-gris']);
  });

  it('una novela sin saga sale con los campos vacíos', () => {
    const n = parseNovel(novelPage, sagas, novels);
    expect(n.saga).toBeNull();
    expect(n.sagaOrder).toBeNull();
    expect(n.related).toEqual([]);
  });

  it('no rompe si no se le pasan los mapas', () => {
    expect(parseNovel(sagaNovelPage).saga).toBeNull();
  });
});

describe('symmetrizeRelated', () => {
  const novel = (slug: string, related: string[]): NovelData => ({
    slug, title: slug, synopsis: '', status: null, categories: [], tags: [],
    featured: false, saga: null, sagaOrder: null, related,
  });

  it('cierra la relación en el sentido que falta', () => {
    const [a, b] = symmetrizeRelated([novel('a', ['b']), novel('b', [])]);
    expect(a.related).toEqual(['b']);
    expect(b.related).toEqual(['a']);
  });

  it('no duplica si ambas ya se declaran', () => {
    const [a, b] = symmetrizeRelated([novel('a', ['b']), novel('b', ['a'])]);
    expect(a.related).toEqual(['b']);
    expect(b.related).toEqual(['a']);
  });

  it('ignora una novela que se relaciona consigo misma', () => {
    const [a] = symmetrizeRelated([novel('a', ['a'])]);
    expect(a.related).toEqual([]);
  });

  it('ignora relaciones a novelas que no están en la lista', () => {
    const [a] = symmetrizeRelated([novel('a', ['inexistente'])]);
    expect(a.related).toEqual([]);
  });
});
```

- [ ] **Step 2: Ejecutar los tests y verificar que fallan**

Run: `cd web && npm test -- transform`
Expected: FAIL — `parseSaga is not a function` (y los `parseNovel con sagas` fallando por campos `undefined`).

- [ ] **Step 3: Ampliar los tipos**

En `web/scripts/notion-sync/types.ts`, añadir `SagaData` y los tres campos de `NovelData`:

```ts
export interface SagaData {
  slug: string;
  name: string;
  description: string;
  order: number;
}

export interface NovelData {
  slug: string;
  title: string;
  synopsis: string;
  status: string | null;
  categories: string[];
  tags: string[];
  featured: boolean;
  saga: string | null;      // slug de la saga, o null
  sagaOrder: number | null; // posición de lectura dentro de la saga
  related: string[];        // slugs de novelas que se cruzan con esta
}
```

- [ ] **Step 4: Implementar en `transform.ts`**

`slugify` y `plainText` ya existen en el fichero (líneas 3-12): reutilízalos, no los redefinas.

Sustituir `parseNovel` (líneas 16-29) y añadir lo demás:

```ts
import type { NotionPage, NovelData, ChapterMeta, SagaData } from './types';

// Resuelve una relación de Notion a slugs, descartando las páginas que no están
// en el mapa (= no publicadas): mismo criterio que un capítulo sin novela.
const relationSlugs = (prop: any, byId?: Map<string, string>): string[] =>
  (prop?.relation ?? [])
    .map((r: any) => byId?.get(r.id))
    .filter((s: string | undefined): s is string => Boolean(s));

export function parseSaga(page: NotionPage): SagaData {
  const p = page.properties;
  const name = plainText(p['Nombre']);
  const rawSlug = plainText(p['Slug']);
  return {
    slug: rawSlug || slugify(name),
    name,
    description: plainText(p['Descripción']),
    order: p['Orden']?.number ?? 0,
  };
}

// El slug de una novela sin resolver relaciones: hace falta para construir el
// mapa id→slug ANTES de poder resolver `Relacionadas`, que apunta a novelas.
export function novelSlugOf(page: NotionPage): string {
  const p = page.properties;
  return plainText(p['Slug']) || slugify(plainText(p['Título']));
}

export function parseNovel(
  page: NotionPage,
  sagaSlugById?: Map<string, string>,
  novelSlugById?: Map<string, string>,
): NovelData {
  const p = page.properties;
  return {
    slug: novelSlugOf(page),
    title: plainText(p['Título']),
    synopsis: plainText(p['Sinopsis']),
    status: p['Estado']?.select?.name ?? null,
    categories: (p['Categorías']?.multi_select ?? []).map((o: any) => o.name),
    tags: (p['Etiquetas']?.multi_select ?? []).map((o: any) => o.name),
    featured: Boolean(p['Destacada']?.checkbox),
    saga: relationSlugs(p['Saga'], sagaSlugById)[0] ?? null,
    sagaOrder: p['Orden en saga']?.number ?? null,
    related: relationSlugs(p['Relacionadas'], novelSlugById),
  };
}

// Notion no simetriza las relaciones a sí misma: si A declara el cruce con B y B
// no declara nada, el lector nunca vería el enlace desde B. Cerramos el grafo.
export function symmetrizeRelated(novels: NovelData[]): NovelData[] {
  const known = new Set(novels.map((n) => n.slug));
  const links = new Map<string, Set<string>>(novels.map((n) => [n.slug, new Set<string>()]));
  for (const n of novels) {
    for (const other of n.related) {
      if (other === n.slug || !known.has(other)) continue;
      links.get(n.slug)!.add(other);
      links.get(other)!.add(n.slug);
    }
  }
  return novels.map((n) => ({ ...n, related: [...links.get(n.slug)!].sort() }));
}
```

- [ ] **Step 5: Ejecutar los tests y verificar que pasan**

Run: `cd web && npm test -- transform`
Expected: PASS — incluidos los tests viejos de `parseNovel`… **salvo uno**. El test «mapea todas las propiedades de una novela» (línea 37) usa `toEqual` con el objeto completo, así que ahora falla porque faltan los tres campos nuevos. Es correcto que falle: actualízalo añadiendo `saga: null, sagaOrder: null, related: []` al objeto esperado.

- [ ] **Step 6: Ejecutar toda la suite**

Run: `cd web && npm test`
Expected: PASS, todo verde.

- [ ] **Step 7: Commit**

```bash
git add web/scripts/notion-sync/types.ts web/scripts/notion-sync/transform.ts web/scripts/notion-sync/transform.test.ts
git commit -m "Sync: parsea sagas, orden de lectura y cruces entre novelas"
```

---

### Task 2: Traer las sagas de Notion y escribirlas al contenido

**Files:**
- Modify: `web/scripts/notion-sync/notion.ts`
- Modify: `web/scripts/notion-sync/writeContent.ts`
- Modify: `web/scripts/notion-sync/index.ts`
- Modify: `web/src/content/config.ts`

**Interfaces:**
- Consumes: `parseSaga`, `novelSlugOf`, `parseNovel`, `symmetrizeRelated`, `SagaData` (Tarea 1).
- Produces:
  - `fetchSagas(): Promise<SagaData[]>`
  - `fetchNovels(sagaSlugById: Map<string,string>): Promise<{ novels: NovelData[]; novelSlugById: Map<string,string> }>`
  - `fetchSagaSlugMap(): Promise<Map<string,string>>`
  - `writeContent(sagas, novels, published, locked)` — **la firma cambia**, `sagas` va primero.
  - Colección Astro `sagas` con `{ slug, name, description, order }`.

**Nota:** `fetchPublishedNovels` y `fetchNovelSlugMap` desaparecen; `fetchNovels` hace el trabajo de las dos con **una sola** query en vez de dos idénticas. Esto no es refactor gratuito: `Relacionadas` apunta a novelas, así que hay que construir el mapa id→slug con las mismas páginas antes de parsearlas.

- [ ] **Step 1: Añadir el ID de la base y las funciones de fetch**

En `web/scripts/notion-sync/notion.ts`, junto a las constantes de las líneas 7-8, añadir el ID que salió de la Tarea 0:

```ts
const SAGAS_DB = 'PEGA-AQUÍ-EL-ID-DE-LA-TAREA-0';
```

Actualizar el import de la línea 3-4:

```ts
import type { NotionPage, NovelData, ChapterData, SagaData } from './types';
import { parseNovel, parseChapterMeta, parseSaga, novelSlugOf, symmetrizeRelated } from './transform';
```

Sustituir `fetchPublishedNovels` y `fetchNovelSlugMap` (líneas 37-47) por:

```ts
export async function fetchSagas(): Promise<SagaData[]> {
  const pages = await queryAll(SAGAS_DB);
  return pages.map(parseSaga).sort((a, b) => a.order - b.order);
}

export async function fetchSagaSlugMap(): Promise<Map<string, string>> {
  const pages = await queryAll(SAGAS_DB);
  return new Map(pages.map((page) => [page.id, parseSaga(page).slug]));
}

export async function fetchNovels(
  sagaSlugById: Map<string, string>,
): Promise<{ novels: NovelData[]; novelSlugById: Map<string, string> }> {
  const pages = await queryAll(NOVELS_DB, PUBLISHED_NOVELS_FILTER);
  // El mapa se construye primero: `Relacionadas` apunta a estas mismas novelas.
  const novelSlugById = new Map(pages.map((page) => [page.id, novelSlugOf(page)]));
  const novels = symmetrizeRelated(
    pages.map((page) => parseNovel(page, sagaSlugById, novelSlugById)),
  );
  return { novels, novelSlugById };
}
```

- [ ] **Step 2: Escribir las sagas al contenido**

En `web/scripts/notion-sync/writeContent.ts`:

```ts
import type { NovelData, ChapterData, SagaData } from './types';

const SAGAS_DIR = join(CONTENT, 'sagas');
```

Cambiar la firma y el barrido de directorios (líneas 17-25):

```ts
export async function writeContent(
  sagas: SagaData[],
  novels: NovelData[],
  published: ChapterData[],
  locked: ChapterData[],
): Promise<void> {
  for (const dir of [SAGAS_DIR, NOVELS_DIR, CHAPTERS_DIR, LOCKED_DIR]) {
    await rm(dir, { recursive: true, force: true });
    await mkdir(dir, { recursive: true });
  }

  for (const s of sagas) {
    await writeFile(join(SAGAS_DIR, `${s.slug}.json`), JSON.stringify(s, null, 2));
  }
```

El resto de la función se queda igual.

- [ ] **Step 3: Encadenarlo en el orquestador**

`web/scripts/notion-sync/index.ts` completo:

```ts
import './env'; // debe ir primero: carga .env antes de que notion.ts lea el token
import { fetchSagas, fetchSagaSlugMap, fetchNovels, fetchChapters } from './notion';
import { writeContent } from './writeContent';

async function main(): Promise<void> {
  const [sagas, sagaSlugById] = await Promise.all([fetchSagas(), fetchSagaSlugMap()]);
  const { novels, novelSlugById } = await fetchNovels(sagaSlugById);
  const chapters = await fetchChapters(novelSlugById);
  const published = chapters.filter((c) => c.unlocked);
  const locked = chapters.filter((c) => !c.unlocked);
  await writeContent(sagas, novels, published, locked);
  console.log(
    `Sync OK: ${sagas.length} sagas, ${novels.length} novelas, ` +
      `${published.length} capítulos publicados, ${locked.length} bloqueados.`,
  );
}

main().catch((err) => {
  console.error('Sync falló:', err);
  process.exit(1);
});
```

- [ ] **Step 4: Declarar la colección en Astro**

En `web/src/content/config.ts`, añadir la colección y los tres campos opcionales. **Los campos van con `.default()`**: sin eso, el JSON de una novela vieja (sin los campos) no valida y el build revienta.

```ts
const sagas = defineCollection({
  type: 'data',
  schema: z.object({
    slug: z.string(),
    name: z.string(),
    description: z.string(),
    order: z.number(),
  }),
});

const novels = defineCollection({
  type: 'data',
  schema: z.object({
    slug: z.string(),
    title: z.string(),
    synopsis: z.string(),
    status: z.string().nullable(),
    categories: z.array(z.string()),
    tags: z.array(z.string()),
    featured: z.boolean(),
    saga: z.string().nullable().default(null),
    sagaOrder: z.number().nullable().default(null),
    related: z.array(z.string()).default([]),
  }),
});

export const collections = { sagas, novels, chapters, lockedChapters };
```

- [ ] **Step 5: Verificar que compila y que la suite sigue verde**

Run: `cd web && npm test && npm run check`
Expected: PASS y 0 errores de tipos. `npm run check` es lo que detecta que la firma de `writeContent` cambió y algún llamador se quedó atrás.

- [ ] **Step 6: Commit**

```bash
git add web/scripts/notion-sync/ web/src/content/config.ts
git commit -m "Sync: trae la base de sagas de Notion y la escribe al contenido"
```

---

### Task 3: Lógica de orden de lectura (pura, testeada)

Antes de pintar nada: cómo se ordenan las novelas de una saga y en qué posición está la que estás leyendo. Todo lo que sigue lo consume.

**Files:**
- Create: `web/src/lib/saga.ts`
- Test: `web/src/lib/saga.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `interface SagaNovel { slug: string; title: string; sagaOrder: number | null }`
  - `readingOrder(novels: SagaNovel[]): SagaNovel[]`
  - `positionIn(slug: string, ordered: SagaNovel[]): string | null` — p. ej. `"2ª de 4"`, o `null` si no se puede situar.

- [ ] **Step 1: Escribir el test que falla**

Crear `web/src/lib/saga.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { readingOrder, positionIn, type SagaNovel } from './saga';

const n = (slug: string, sagaOrder: number | null, title = slug): SagaNovel =>
  ({ slug, title, sagaOrder });

describe('readingOrder', () => {
  it('ordena por sagaOrder', () => {
    const out = readingOrder([n('c', 3), n('a', 1), n('b', 2)]);
    expect(out.map((x) => x.slug)).toEqual(['a', 'b', 'c']);
  });

  it('manda al final las que no tienen orden, alfabéticas entre sí', () => {
    const out = readingOrder([n('z', null, 'Zafiro'), n('a', null, 'Alba'), n('b', 1)]);
    expect(out.map((x) => x.slug)).toEqual(['b', 'a', 'z']);
  });

  it('no muta la lista original', () => {
    const input = [n('b', 2), n('a', 1)];
    readingOrder(input);
    expect(input.map((x) => x.slug)).toEqual(['b', 'a']);
  });
});

describe('positionIn', () => {
  const ordered = readingOrder([n('a', 1), n('b', 2), n('c', 3)]);

  it('describe la posición en femenino, como "novela"', () => {
    expect(positionIn('b', ordered)).toBe('2ª de 3');
  });

  it('devuelve null si la novela no está en la saga', () => {
    expect(positionIn('x', ordered)).toBeNull();
  });

  it('devuelve null si la saga tiene una sola novela: "1ª de 1" no informa de nada', () => {
    expect(positionIn('a', readingOrder([n('a', 1)]))).toBeNull();
  });
});
```

- [ ] **Step 2: Ejecutar el test y verificar que falla**

Run: `cd web && npm test -- saga`
Expected: FAIL — `Failed to resolve import "./saga"`.

- [ ] **Step 3: Implementar `web/src/lib/saga.ts`**

```ts
export interface SagaNovel {
  slug: string;
  title: string;
  sagaOrder: number | null;
}

// Las que no tienen orden van al final: una saga a medio numerar no debe
// colarse por delante de la primera novela.
export function readingOrder(novels: SagaNovel[]): SagaNovel[] {
  return [...novels].sort((a, b) => {
    if (a.sagaOrder === null && b.sagaOrder === null) {
      return a.title.localeCompare(b.title, 'es');
    }
    if (a.sagaOrder === null) return 1;
    if (b.sagaOrder === null) return -1;
    return a.sagaOrder - b.sagaOrder;
  });
}

export function positionIn(slug: string, ordered: SagaNovel[]): string | null {
  if (ordered.length < 2) return null; // "1ª de 1" no le dice nada al lector
  const i = ordered.findIndex((n) => n.slug === slug);
  return i === -1 ? null : `${i + 1}ª de ${ordered.length}`;
}
```

- [ ] **Step 4: Ejecutar el test y verificar que pasa**

Run: `cd web && npm test -- saga`
Expected: PASS, 6 tests.

- [ ] **Step 5: Commit**

```bash
git add web/src/lib/saga.ts web/src/lib/saga.test.ts
git commit -m "Orden de lectura de una saga"
```

---

### Task 4: Página `/saga/[slug]`

**Files:**
- Create: `web/src/pages/saga/[slug].astro`

**Interfaces:**
- Consumes: colección `sagas` (Tarea 2), `readingOrder` (Tarea 3), `NovelCard`, `Breadcrumb`.
- Produces: la ruta `/saga/<slug>`, a la que enlazan las Tareas 5 y 6.

Sigue el patrón de `web/src/pages/etiqueta/[slug].astro` — mismos estilos, mismo `NovelCard`.

- [ ] **Step 1: Crear la página**

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import NovelCard from '../../components/NovelCard.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
import { readingOrder } from '../../lib/saga';

export async function getStaticPaths() {
  const sagas = await getCollection('sagas');
  return sagas.map((s) => ({
    params: { slug: s.data.slug },
    props: { name: s.data.name, description: s.data.description },
  }));
}

interface Props { name: string; description: string; }
const { name, description } = Astro.props;
const { slug } = Astro.params;

const novels = (await getCollection('novels')).filter((n) => n.data.saga === slug);
const ordered = readingOrder(
  novels.map((n) => ({ slug: n.data.slug, title: n.data.title, sagaOrder: n.data.sagaOrder })),
);
const bySlug = new Map(novels.map((n) => [n.data.slug, n.data]));
---

<BaseLayout title={`Saga ${name} — Draveir`} description={description}>
  <main class="container wide" id="contenido">
    <Breadcrumb items={[{ label: 'Novelas', href: '/novelas' }, { label: name }]} />
    <div class="page-head">
      <span class="eyebrow">Saga</span>
      <h1>{name}</h1>
      {description && <p class="saga-desc">{description}</p>}
    </div>
    <ol class="novel-grid">
      {ordered.map((o) => {
        const n = bySlug.get(o.slug)!;
        return (
          <NovelCard
            slug={n.slug}
            title={n.title}
            synopsis={n.synopsis}
            status={n.status}
            categories={n.categories}
          />
        );
      })}
    </ol>
  </main>
</BaseLayout>

<style>
  .wide { max-width: 72rem; }
  .page-head { margin-bottom: var(--space-5); }
  .page-head h1 { margin: var(--space-1) 0 0; }
  .saga-desc { color: var(--text-muted); font-size: 1.1rem; max-width: 60ch; margin-top: var(--space-3); }
  .novel-grid {
    list-style: none; padding: 0; display: grid; gap: var(--space-5);
    grid-template-columns: repeat(auto-fill, minmax(230px, 1fr));
  }
</style>
```

`<ol>` y no `<ul>` como en `/etiqueta`: aquí el orden **significa** algo, y eso es exactamente lo que distingue una lista ordenada de una a secas. `NovelCard` ya emite su propio `<li>`.

- [ ] **Step 2: Verificar que compila**

Run: `cd web && npm run check && npm run build`
Expected: 0 errores. Si aún no hay sagas sincronizadas, `getStaticPaths` devuelve `[]` y no se genera ninguna página — es correcto, no es un fallo.

- [ ] **Step 3: Commit**

```bash
git add web/src/pages/saga/
git commit -m "Página de saga: el mundo y sus novelas en orden de lectura"
```

---

### Task 5: La saga en la ficha de novela

**Files:**
- Modify: `web/src/pages/novela/[slug].astro`

**Interfaces:**
- Consumes: `readingOrder`, `positionIn` (Tarea 3); la ruta `/saga/<slug>` (Tarea 4).
- Produces: nada que otras tareas consuman.

- [ ] **Step 1: Cargar la saga en el frontmatter**

En `web/src/pages/novela/[slug].astro`, añadir el import junto a los de las líneas 7-9:

```ts
import { readingOrder, positionIn } from '../../lib/saga';
```

Y tras `const gradient = coverGradient(...)` (línea 48):

```ts
const saga = novel.data.saga ? await getEntry('sagas', novel.data.saga) : undefined;
const sagaPosition = saga
  ? positionIn(
      novel.data.slug,
      readingOrder(
        (await getCollection('novels'))
          .filter((n) => n.data.saga === novel.data.saga)
          .map((n) => ({ slug: n.data.slug, title: n.data.title, sagaOrder: n.data.sagaOrder })),
      ),
    )
  : null;
```

- [ ] **Step 2: Pintar la línea bajo el título**

Dentro de `<div class="meta">` (líneas 72-77), como primer hijo, antes del `{novel.data.status && ...}`:

```astro
{saga && (
  <a class="saga-line" href={`/saga/${saga.data.slug}`}>
    {sagaPosition && <span class="saga-pos">{sagaPosition}</span>}
    Saga {saga.data.name}
  </a>
)}
```

Y al bloque `<style>` del final:

```css
  .saga-line { display: inline-flex; align-items: center; gap: 0.4rem; color: var(--brass); text-decoration: none; }
  .saga-line:hover { text-decoration: underline; }
  .saga-pos { color: var(--text-muted); font-size: 0.85em; }
```

- [ ] **Step 3: Verificar**

Run: `cd web && npm run check && npm run build`
Expected: 0 errores. `el-heraldo-gris` (sin saga) sigue construyéndose y **no** muestra la línea: ese es el canario de los campos opcionales.

- [ ] **Step 4: Commit**

```bash
git add web/src/pages/novela/\[slug\].astro
git commit -m "Ficha de novela: a qué saga pertenece y en qué posición"
```

---

### Task 6: El bloque de descubrimiento al final del último capítulo publicado

El punto del rediseño entero: el lector acaba de quedarse sin nada que leer.

**Files:**
- Create: `web/src/components/SagaFooter.astro`
- Modify: `web/src/pages/novela/[slug]/[capitulo].astro`

**Interfaces:**
- Consumes: `readingOrder` (Tarea 3); la ruta `/saga/<slug>` (Tarea 4).
- Produces: `<SagaFooter novelSlug={string} />` — se renderiza a nada si la novela no tiene saga ni relacionadas.

- [ ] **Step 1: Crear el componente**

`web/src/components/SagaFooter.astro`. Encapsula toda la carga: la página de capítulo ya tiene bastante frontmatter.

```astro
---
import { getCollection, getEntry } from 'astro:content';
import { readingOrder } from '../lib/saga';

interface Props { novelSlug: string; }
const { novelSlug } = Astro.props;

const all = await getCollection('novels');
const novel = all.find((n) => n.data.slug === novelSlug);

const saga = novel?.data.saga ? await getEntry('sagas', novel.data.saga) : undefined;

// Hermanas: las demás novelas de la saga, en orden de lectura.
const siblings = saga
  ? readingOrder(
      all
        .filter((n) => n.data.saga === saga.data.slug && n.data.slug !== novelSlug)
        .map((n) => ({ slug: n.data.slug, title: n.data.title, sagaOrder: n.data.sagaOrder })),
    )
  : [];

// La "siguiente": la primera hermana con orden mayor que el de esta novela.
const myOrder = novel?.data.sagaOrder ?? null;
const nextSlug =
  myOrder === null
    ? undefined
    : siblings.find((s) => s.sagaOrder !== null && s.sagaOrder > myOrder)?.slug;

const relatedSlugs = novel?.data.related ?? [];
const related = relatedSlugs
  .map((slug) => all.find((n) => n.data.slug === slug))
  .filter((n): n is NonNullable<typeof n> => Boolean(n));

const hasSomething = siblings.length > 0 || related.length > 0;
---

{hasSomething && (
  <aside class="saga-foot">
    <h2>Sigue leyendo en este mundo</h2>

    {saga && siblings.length > 0 && (
      <Fragment>
        <p class="saga-intro">
          Esta historia es parte de <a href={`/saga/${saga.data.slug}`}>{saga.data.name}</a>.
          {saga.data.description && ` ${saga.data.description}`}
        </p>
        <ul>
          {siblings.map((s) => (
            <li>
              <a href={`/novela/${s.slug}`}>
                {s.slug === nextSlug && <span class="next">Siguiente</span>}
                {s.title}
              </a>
            </li>
          ))}
        </ul>
      </Fragment>
    )}

    {related.length > 0 && (
      <Fragment>
        <p class="saga-intro">También se cruza con:</p>
        <ul>
          {related.map((n) => (
            <li><a href={`/novela/${n.data.slug}`}>{n.data.title}</a></li>
          ))}
        </ul>
      </Fragment>
    )}
  </aside>
)}

<style>
  .saga-foot {
    margin-top: var(--space-6);
    padding-top: var(--space-5);
    border-top: 1px solid var(--line);
  }
  .saga-foot h2 { margin: 0 0 var(--space-3); }
  .saga-intro { color: var(--text-muted); max-width: 60ch; }
  .saga-foot ul { list-style: none; padding: 0; margin: var(--space-3) 0 var(--space-4); }
  .saga-foot li { margin-bottom: var(--space-2); }
  .saga-foot li a { display: inline-flex; align-items: center; gap: 0.5rem; }
  .next {
    font-size: 0.7rem; text-transform: uppercase; letter-spacing: 0.08em;
    color: var(--brass); border: 1px solid var(--brass);
    border-radius: 999px; padding: 0.1rem 0.45rem;
  }
</style>
```

- [ ] **Step 2: Colgarlo del último capítulo publicado**

En `web/src/pages/novela/[slug]/[capitulo].astro`, añadir el import junto a los de las líneas 2-7:

```ts
import SagaFooter from '../../../components/SagaFooter.astro';
```

Tras `const minutes = ...` (línea 44), calcular si este es el último publicado. **`pub` (línea 33) ya tiene los capítulos publicados de esta novela** — reutilízalo, no vuelvas a llamar a `getCollection`:

```ts
// El último capítulo PUBLICADO, aunque exista uno bloqueado por encima: ese es
// justo el caso "alcanzaste al autor", y también merece el bloque.
const lastPublished = Math.max(0, ...pub.map((c) => c.data.number));
const isLastPublished =
  props.kind === 'published' && props.entry.data.number === lastPublished;
```

En el markup, entre el `</article>` y `<div class="chapter-foot">` (líneas 69-71) — es decir, dentro del bloque que solo se pinta en capítulos publicados, tras el `)}` del ternario:

```astro
    {isLastPublished && <SagaFooter novelSlug={slug} />}

    <div class="chapter-foot">
```

- [ ] **Step 3: Verificar**

Run: `cd web && npm run check && npm run build`
Expected: 0 errores.

- [ ] **Step 4: Verificación end-to-end contra Notion (la de verdad)**

Esta es la única que prueba que las propiedades de Notion se llaman como el código cree.

```bash
cd web && npm run sync && npm run build && npm run preview
```

Comprobar en el navegador:
1. `src/content/sagas/` tiene un JSON por saga.
2. El JSON de una novela con saga trae `saga`, `sagaOrder` y `related` llenos; el de `el-heraldo-gris` los trae en `null`/`[]` y **su página sigue funcionando**.
3. `/saga/<slug>` lista las novelas en orden de lectura.
4. La ficha de una novela con saga muestra «2ª de 4 · Saga …» enlazada.
5. En el **último capítulo publicado** de una novela con saga aparece «Sigue leyendo en este mundo» con las hermanas; en un capítulo intermedio, **no**.
6. Si hay `Relacionadas`, el cruce se ve **desde las dos novelas** (esa es la simetría de la Tarea 1).

- [ ] **Step 5: Commit**

```bash
git add web/src/components/SagaFooter.astro "web/src/pages/novela/[slug]/[capitulo].astro"
git commit -m "Al quedarse sin capítulos, el lector descubre el resto del mundo"
```

---

## Fuera de alcance

Del spec, y no se hace aquí: novelas en varias sagas (tabla intermedia), índice `/sagas`, emblema propio de la saga, saga en capítulos.
